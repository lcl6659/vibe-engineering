FROM node:20-alpine

WORKDIR /app

# 复制 package 文件
COPY package.json package-lock.json* ./

# 安装依赖
RUN npm ci

# 复制源代码
COPY . .

# 暴露端口
EXPOSE 3000

# 使用开发模式运行（Next.js 会自动检测文件变化并热重载）
CMD ["npm", "run", "dev"]

